import type { Metadata } from "next";
import { Poppins } from "next/font/google";
import "./globals.css";

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["100", "200", "300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "Restu Sibuea - Junior Web Developer",
  description: "Portfolio of Restu Sibuea, a junior web developer from Medan, specializing in frontend and backend development.",
  viewport: "width=device-width, initial-scale=1, maximum-scale=1",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${poppins.className} bg-dark text-white transition-colors duration-300`}>
        {children}
      </body>
    </html>
  );
}
